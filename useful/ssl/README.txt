SSL certificates go here, in the following format:
server.key - Private key for the server
server.pem - Server certificate (public key)

They are automatically picked up by nginx in the Docker container.
